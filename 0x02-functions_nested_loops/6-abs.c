#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 * Challenge: comment on all variables
 *@ab: the integer to check
 * Return: absolute value of int
**/

int _abs(int ab)
{
	if (ab >= 0)
	{
		return (ab);
	}
	return (-ab);
}
