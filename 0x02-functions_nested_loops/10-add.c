#include "main.h"

/**
 * add - adds two integers and returns the result
 * Challenge: use global variables are prohibited
 * @fn: int to be added to first input
 * @sn: int to be added to second input
 * Return: total
**/

int add(int fn, int sn)
{
	int total;

	total = fn + sn;

	return (total)
}
