#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 * Challenge: change only 1 line in this task
 * Return: 0 if result is as expected else !0
**/

int main(void)
{
int i;

i = 0;
positive_or_negative(i);

return (0);
}
